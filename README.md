# Тестовое задание "DevOps-инженер (Яндекс Браузер)"

В этом репозитории выполнено тестовое задание, состоящее из 3 блоков (A, B).

## 📂 Структура проекта

*   `test-project/` - **Задание А1**. Docker-проект с Nginx.
    *   `Dockerfile` - инструкция сборки образа.
    *   `docker-compose.yml` - файл для быстрого запуска.
    *   `index.html` - веб-страница.
*   `scripts/` - **Задание B1**. Bash-скрипт.
    *   `clean_old_logs.sh` - скрипт очистки логов.
*   `ANSWERS.md` - **Задания B2 и B3**. Текстовые ответы на вопросы по Git и CI/CD.

---

## 🚀 Как проверить выполнение

### 1. Проверка Docker (Задание А1)

Требуется установленный Docker и Docker Compose.

1.  Перейдите в папку проекта:
    ```bash
    cd test-project
    ```
2.  Запустите контейнер:
    ```bash
    docker-compose up -d
    ```
3.  Откройте браузер по адресу: [http://localhost:8080](http://localhost:8080)
    *   Вы должны увидеть надпись "Hello from [Ваше Имя]"
4.  Остановить проект:
    ```bash
    docker-compose down
    ```

### 2. Проверка Bash-скрипта (Задание B1)

Скрипту нужны права на выполнение (Linux/macOS/WSL).

1.  Перейдите в папку скриптов:
    ```bash
    cd scripts
    ```
2.  (Опционально) Создайте тестовые логи для проверки:
    ```bash
    mkdir test_logs
    touch test_logs/old.log -d "10 days ago" # Создаем "старый" файл
    touch test_logs/new.log                  # Создаем "новый" файл
    ```
3.  Запустите скрипт (поиск файлов старше 7 дней):
    ```bash
    chmod +x clean_old_logs.sh
    ./clean_old_logs.sh test_logs 7
    ```
4.  Скрипт найдет `old.log` и спросит подтверждение на удаление.

### 3. Ответы на вопросы (Задания B2, B3)

Откройте файл [ANSWERS.md](ANSWERS.md) для просмотра ответов на вопросы про Git и CI/CD.
